{
  "name": "hello-postinstall-demo-monorepo",
  "private": true,
  "scripts": {
    "dev": "cd apps/hello-postinstall && vercel dev",
    "deploy": "cd apps/hello-postinstall && vercel deploy",
    "deploy:prod": "cd apps/hello-postinstall && vercel deploy --prod",
    "install:import-test": "NODE_ENV=development pnpm install --filter import-test...",
    "install:import-test:prod": "NODE_ENV=production pnpm install --filter import-test...",
    "test:import": "pnpm --filter import-test test",
    "changeset": "changeset",
    "version": "changeset version",
    "release": "changeset publish"
  },
  "devDependencies": {
    "@changesets/changelog-github": "0.6.0",
    "@changesets/cli": "2.31.0"
  }
}
